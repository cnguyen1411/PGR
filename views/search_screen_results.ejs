<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles.css">
  <title>Search Screen Results</title>
</head>
<body>
  <header>
    <%- include('navbar', { page: 'search_screen' }) %>
  </header>
  <main>
    <div class="container">
      <h1>Search Screen Results</h1>
      <% if (results.length > 0) { %>
        <table>
          <thead>
            <tr>
              <th>Customer Name</th>
              <th>Tracking Number</th>
              <th>Date Accepted</th>
              <th>Model</th>
              <th>Quantity</th>
              <th>Print</th>
            </tr>
          </thead>
          <tbody>
            <% results.forEach(result => { %>
              <tr>
                <td><%= result.customerName %></td>
                <td><%= result.trackingNumber %></td>
                <td><%= result.dateAccepted %></td>
                <td><%= result.model %></td>
                <td><%= result.quantity %></td>
                <td><a class="button-link" href="/device/print_screen/<%= result.model %>/<%= result.trackingNumber %>" target="_blank">Print</a></td>
              </tr>
            <% }); %>
          </tbody>
        </table>
      <% } else { %>
        <p>No results found.</p>
      <% } %>
      <a class="button-link-back" href="/device/search_screen">Back to Search</a>
    </div>
  </main>
  <footer>
    <p>© 2024 PHONEGLASSREPAIR. All rights reserved.</p>
  </footer>
</body>
</html>
