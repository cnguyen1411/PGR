<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles.css">
  <title>Success Screen</title>
</head>
<body>
  <header>
    <%- include('navbar', { page: 'input' }) %>
  </header>
  <main>
    <div class="container">
      <div class="table-container">
        <h1>Successfully Saved, Click Print to print label</h1>
        <table>
          <thead>
            <tr>
              <th>Customer Name</th>
              <th>Tracking Number</th>
              <th>Date Accepted</th>
              <th>Model</th>
              <th>Quantity</th>
              <th>Print</th>
            </tr>
          </thead>
          <tbody>
            <% results.forEach(result => { %>
              <tr>
                <td><%= result.customerName %></td>
                <td><%= result.trackingNumber %></td>
                <td><%= result.dateAccepted %></td>
                <td><%= result.model %></td>
                <td><%= result.quantity %></td>
                <td><a class="button-link" href="/device/print_screen/<%= result.trackingNumber %>" target="_blank">Print</a></td>
              </tr>
            <% }); %>
          </tbody>
        </table>
        <a href="/device/input">Back to Input Form</a>
      </div>
    </div>
  </main>
  <footer>
    <p>© 2024 PHONEGLASSREPAIR. All rights reserved.</p>
  </footer>
</body>
</html>
